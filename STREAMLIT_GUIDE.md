# 🎨 Streamlit 앱 사용 가이드

## 실행 방법

```bash
cd /Users/gyuteoi/Desktop/graphRAG
source venv/bin/activate
streamlit run src/streamlit_app.py
```

브라우저에서 자동으로 열립니다: `http://localhost:8501`

## 주요 기능

### 1. 💬 질문하기 탭

#### 모드 선택
- **🌐 OpenAI API (정확)**: OpenAI의 gpt-4o-mini 모델 사용
  - 장점: 더 정확한 답변
  - 단점: 유료 (API 키 필요)
  
- **💻 Ollama 로컬 (빠름)**: 로컬 Ollama 모델 사용
  - 장점: 무료, 빠른 응답
  - 단점: Ollama 서버가 실행 중이어야 함

#### 사용 방법
1. 사이드바에서 원하는 모드 선택
2. 질문 입력 (예: "What is NVIDIA revenue?")
3. "질문하기" 버튼 클릭
4. AI 답변 확인

### 2. 📝 텍스트 추가 탭

#### 텍스트 인덱싱
- 새로운 텍스트를 그래프에 추가
- OpenAI API를 사용해 정확하게 인덱싱
- 엔티티와 관계를 자동으로 추출

#### 사용 방법
1. 텍스트 입력 (금융 보고서, 뉴스 등)
2. "추가하기" 버튼 클릭
3. 인덱싱 완료 대기 (시간이 걸릴 수 있음)

### 3. 🎨 그래프 시각화 탭 (NEW!)

#### 실시간 그래프 보기
- 현재 그래프 구조를 인터랙티브하게 확인
- `graph_ui.html` 파일을 자동으로 로드
- 파일이 업데이트되면 새로고침으로 반영

#### 인터랙티브 기능
- **드래그**: 노드를 클릭하고 드래그해서 이동
- **확대/축소**: 마우스 휠로 확대/축소
- **하이라이트**: 노드 클릭 시 연결된 노드 강조

#### 그래프 관리
- **🔄 새로고침**: 최신 그래프 상태 반영
- **🎨 그래프 재생성**: `visualize.py` 실행해서 새로 생성
- **📊 그래프 통계 보기**: 노드/엣지 수 확인

#### 자동 업데이트
- 파일 추가 시 자동으로 반영됨
- 마지막 업데이트 시간 표시
- 새로고침 버튼으로 즉시 반영

## 사이드바 정보

### 📊 시스템 상태
- 서버 연결 상태 (✅/❌)
- 그래프 통계 (노드 수, 엣지 수)

### ⚙️ 설정
- 질문 모드 선택 (API/Local)
- 모드별 설명 및 안내

### 🎨 그래프 시각화
- 그래프 새로고침 버튼
- 최신 상태로 업데이트

## 실시간 반영 흐름

```
[1] 텍스트 추가
    ↓
[2] GraphRAG 인덱싱
    ↓
[3] 그래프 파일 업데이트 (graph_chunk_entity_relation.graphml)
    ↓
[4] visualize.py 실행 (수동 또는 자동)
    ↓
[5] graph_ui.html 생성/업데이트
    ↓
[6] Streamlit에서 🔄 새로고침 클릭
    ↓
[7] 최신 그래프 시각화 확인!
```

## 예제 사용 시나리오

### 시나리오 1: 새 정보 추가 및 확인
1. "📝 텍스트 추가" 탭에서 뉴스 기사 추가
2. 인덱싱 완료 대기
3. "🎨 그래프 시각화" 탭으로 이동
4. "🎨 그래프 재생성" 클릭
5. 새로운 노드와 엣지 확인
6. "💬 질문하기" 탭에서 추가한 내용 질문

### 시나리오 2: 모드 비교
1. "💬 질문하기" 탭에서 질문 입력
2. "🌐 OpenAI API" 모드로 질문
3. 답변 확인
4. 사이드바에서 "💻 Ollama 로컬" 모드로 변경
5. 같은 질문으로 답변 비교

### 시나리오 3: 그래프 탐색
1. "🎨 그래프 시각화" 탭 열기
2. 노드를 드래그해서 구조 파악
3. 특정 노드 클릭해서 연결 확인
4. 관심 있는 엔티티 발견
5. "💬 질문하기" 탭에서 해당 엔티티 질문

## 문제 해결

### 그래프가 표시되지 않아요
```bash
# 그래프 파일 생성
cd /Users/gyuteoi/Desktop/graphRAG
source venv/bin/activate
python3 src/visualize.py
```

### Streamlit이 시작되지 않아요
```bash
# 포트 확인
lsof -ti:8501 | xargs kill -9

# 다시 시작
streamlit run src/streamlit_app.py
```

### 서버 연결 실패
```bash
# FastAPI 서버 시작
cd /Users/gyuteoi/Desktop/graphRAG
source venv/bin/activate
python3 src/app.py
```

## 팁

1. **빠른 테스트**: Local 모드로 빠르게 테스트 후 API 모드로 정확한 답변 확인
2. **그래프 확인**: 텍스트 추가 후 그래프 시각화로 어떻게 연결되었는지 확인
3. **새로고침**: 그래프가 업데이트되지 않으면 새로고침 버튼 클릭
4. **모드 전환**: 질문 전에 원하는 모드로 먼저 전환하기

## 단축키

- `Ctrl + R` (또는 `Cmd + R`): 페이지 새로고침
- `Ctrl + Shift + R`: 캐시 무시하고 새로고침

