# 🎨 Streamlit 앱 주요 기능

## 실행 방법

```bash
cd /Users/gyuteoi/Desktop/graphRAG
source venv/bin/activate
streamlit run src/streamlit_app.py
```

브라우저: `http://localhost:8501`

---

## 📝 데이터 추가 탭 (업데이트!)

### 3가지 입력 방법

#### 1. 텍스트 직접 입력
- 텍스트를 직접 입력해서 인덱싱
- 뉴스, 보고서 내용 등을 복사-붙여넣기
- **자동 그래프 재생성**: 인덱싱 완료 후 자동으로 그래프 업데이트!

#### 2. PDF 업로드
- PDF 파일을 업로드해서 자동 처리
- PDF → 텍스트 추출 → 인덱싱 → 그래프 생성
- **자동 그래프 재생성**: PDF 처리 완료 후 자동으로 그래프 업데이트!
- 지원 형식: `.pdf`

**사용 예시**:
```
1. "PDF 업로드" 선택
2. PDF 파일 드래그 앤 드롭 또는 선택
3. "PDF 추가하기" 버튼 클릭
4. 자동으로 처리:
   - PDF 텍스트 추출
   - GraphRAG 인덱싱
   - 그래프 재생성
5. "그래프 시각화" 탭에서 확인!
```

#### 3. URL 크롤링
- 웹 페이지 URL을 입력해서 자동 크롤링
- URL → 텍스트 추출 → 인덱싱 → 그래프 생성
- **자동 그래프 재생성**: 크롤링 완료 후 자동으로 그래프 업데이트!

**사용 예시**:
```
1. "URL 크롤링" 선택
2. URL 입력 (예: https://www.nvidia.com/news)
3. "URL 추가하기" 버튼 클릭
4. 자동으로 처리:
   - 웹 페이지 크롤링
   - 텍스트 추출
   - GraphRAG 인덱싱
   - 그래프 재생성
5. "그래프 시각화" 탭에서 확인!
```

---

## 💬 질문하기 탭

### 모드 선택 (사이드바)
- **🌐 OpenAI API (정확)**: gpt-4o-mini 사용
  - 더 정확한 답변
  - 유료 (API 키 필요)
  
- **💻 Ollama 로컬 (빠름)**: llama3.2:3b 사용
  - 무료
  - Ollama 서버 필요

### 사용 방법
1. 사이드바에서 모드 선택
2. 질문 입력
3. "질문하기" 버튼 클릭
4. AI 답변 확인

---

## 🎨 그래프 시각화 탭

### 실시간 그래프 보기
- `graph_ui.html` 파일을 자동으로 로드
- 인터랙티브 그래프 뷰어
- **자동 업데이트**: 데이터 추가 시 자동으로 그래프 재생성!

### 인터랙티브 기능
- **드래그**: 노드 이동
- **확대/축소**: 마우스 휠
- **하이라이트**: 노드 클릭

### 그래프 관리
- **🔄 새로고침**: 최신 그래프 상태 반영
- **🎨 그래프 재생성**: 수동으로 재생성
- **📊 그래프 통계 보기**: 노드/엣지 수 확인

---

## 자동 그래프 재생성 흐름

### 텍스트 직접 입력
```
텍스트 입력 
  ↓
"텍스트 추가하기" 클릭
  ↓
GraphRAG 인덱싱 (OpenAI API)
  ↓
✅ 인덱싱 완료
  ↓
🎨 그래프 자동 재생성 (visualize.py 실행)
  ↓
✅ 그래프 업데이트 완료
  ↓
"그래프 시각화" 탭에서 확인!
```

### PDF 업로드
```
PDF 파일 선택
  ↓
"PDF 추가하기" 클릭
  ↓
PDF → 텍스트 추출 (PyMuPDF)
  ↓
GraphRAG 인덱싱 (OpenAI API)
  ↓
✅ 인덱싱 완료
  ↓
🎨 그래프 자동 재생성 (visualize.py 실행)
  ↓
✅ 그래프 업데이트 완료
  ↓
"그래프 시각화" 탭에서 확인!
```

### URL 크롤링
```
URL 입력
  ↓
"URL 추가하기" 클릭
  ↓
웹 페이지 크롤링 (requests + BeautifulSoup)
  ↓
텍스트 추출
  ↓
GraphRAG 인덱싱 (OpenAI API)
  ↓
✅ 인덱싱 완료
  ↓
🎨 그래프 자동 재생성 (visualize.py 실행)
  ↓
✅ 그래프 업데이트 완료
  ↓
"그래프 시각화" 탭에서 확인!
```

---

## 사이드바 정보

### 📊 시스템 상태
- 서버 연결 상태 (✅/❌)
- 그래프 통계 (노드 수, 엣지 수)

### ⚙️ 설정
- 질문 모드 선택 (API/Local)
- 모드별 설명

### 🎨 그래프 시각화
- 그래프 새로고침 버튼

---

## 예제 시나리오

### 시나리오 1: PDF 분석
```
1. "데이터 추가" 탭 → "PDF 업로드" 선택
2. 금융 보고서 PDF 업로드
3. "PDF 추가하기" 클릭
4. 자동 처리 대기 (1-3분)
5. "그래프 시각화" 탭에서 새로운 노드 확인
6. "질문하기" 탭에서 보고서 내용 질문
```

### 시나리오 2: 뉴스 크롤링
```
1. "데이터 추가" 탭 → "URL 크롤링" 선택
2. 뉴스 기사 URL 입력
3. "URL 추가하기" 클릭
4. 자동 처리 대기 (30초-1분)
5. "그래프 시각화" 탭에서 새로운 연결 확인
6. "질문하기" 탭에서 뉴스 내용 질문
```

### 시나리오 3: 텍스트 직접 입력
```
1. "데이터 추가" 탭 → "텍스트 직접 입력" 선택
2. 텍스트 복사-붙여넣기
3. "텍스트 추가하기" 클릭
4. 자동 처리 대기 (30초-2분)
5. "그래프 시각화" 탭에서 업데이트 확인
```

---

## 문제 해결

### PDF 업로드가 안 돼요
```bash
# PyMuPDF 설치 확인
pip install PyMuPDF
```

### URL 크롤링이 안 돼요
```bash
# requests, beautifulsoup4 설치 확인
pip install requests beautifulsoup4
```

### 그래프가 업데이트되지 않아요
```
1. "그래프 시각화" 탭에서 "🔄 새로고침" 클릭
2. 또는 "🎨 그래프 재생성" 클릭
```

### 서버 연결 실패
```bash
# FastAPI 서버 시작
cd /Users/gyuteoi/Desktop/graphRAG
source venv/bin/activate
python3 src/app.py
```

---

## 팁

1. **빠른 테스트**: 텍스트 직접 입력으로 빠르게 테스트
2. **PDF 처리**: 긴 보고서는 PDF 업로드로 자동 처리
3. **뉴스 수집**: URL 크롤링으로 최신 뉴스 자동 수집
4. **그래프 확인**: 데이터 추가 후 자동으로 그래프가 업데이트됨!
5. **모드 전환**: 빠른 답변은 Local, 정확한 답변은 API 모드

---

## 주요 업데이트

### v2.1 (최신)
- ✅ PDF 업로드 기능 추가
- ✅ URL 크롤링 기능 추가
- ✅ 데이터 추가 시 자동 그래프 재생성
- ✅ 3가지 입력 방법 통합 UI

### v2.0
- ✅ 모드 선택 기능 (API/Local)
- ✅ 그래프 시각화 탭
- ✅ 실시간 그래프 표시

